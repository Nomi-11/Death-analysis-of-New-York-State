# Data

## Description

## Research plan

## Missing value analysis

```{r}
library(RColorBrewer)
library(ggplot2)
death_data <- read.csv("Death_data_NY.csv")
sum(is.na(death_data)) #There is no NA data in the dataset

# pie
race_counts <- as.data.frame(table(death_data$Race.or.Ethnicity))

ggplot(race_counts, aes(x="", y=Freq, fill=Var1)) +
  geom_bar(stat="identity", width=1) +
  coord_polar("y", start=0) +
  theme_minimal() +  # Using a minimal theme
  theme(axis.line = element_blank(),
        axis.text = element_blank(),
        axis.title = element_blank(),
        panel.border = element_blank(),
        panel.grid = element_blank(),
        axis.ticks = element_blank(),
        plot.title = element_text(size=22)) +
  scale_fill_brewer(palette="Pastel1") +  # Custom color palette
  labs(fill="Race or Ethnicity", title="Pie Chart of Race or Ethnicity") +
  guides(fill=guide_legend(title="Race or Ethnicity"))
# color_palette <- brewer.pal(n = length(race_counts), name = "Set3")
# pie(race_counts, col = color_palette ,labels = names(race_counts), main = "Pie Chart of 'Race or Ethnicity'")


# bar
table(death_data$Age.Group)
```
